export interface Mappins {
    createdAt: string
    updatedAt: string
    mapboxId: string
    latitude: number
    longitude: number
    userId: {
        createdAt: string
        username: string
        color: string
        _id: string
    }
    _id: string
}
